<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DynamicColors.Light"
        tools:targetApi="31">
        <activity
            android:name=".UpdateActivity"
            android:exported="false" />
        <activity
            android:name=".ReConfirmDataActivity"
            android:exported="false"
            android:label="ConfirmData" />
        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="History" /> <!-- <activity -->
        <!-- android:name=".HIstoryActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".InsightView"
            android:exported="false"
            android:label="My Wallet" /> <!-- <activity -->
        <!-- android:name=".MoneyActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".AddFund"
            android:exported="false"
            android:label="My Wallet" />
        <activity
            android:name=".Expenses_List"
            android:exported="false"
            android:label="My Wallet" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="My Wallet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>